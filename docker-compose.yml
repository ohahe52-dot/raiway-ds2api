version: '3.8'

services:
  ds2api:
    image: cjackhwang/ds2api:latest
    container_name: ds2api
    ports:
      - "5001:5001"
    environment:
      - ADMIN_KEY=your_strong_admin_password_here
      - CONFIG_BASE64=your_base64_encoded_config_here
    restart: unless-stopped
